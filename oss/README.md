# OSS

Track third-party open-source candidates in `oss/projects.yaml`.

Local clones belong under `oss/clones/` and are gitignored. Do not commit third-party source into this template unless a human explicitly decides to vendor it.
