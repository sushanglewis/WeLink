# Feature Catalog: Baserow + Mattermost 督办协作系统

## P0 功能清单与验收标准

### FR-001 ~ FR-006：Excel 解析与录入

| 编号 | 功能 | 验收标准 |
|------|------|----------|
| FR-001 | Agent 调用脚本导入 Excel | Agent 通过 `upload-excel --table-id 378 --file path.xlsx --operation append` 触发导入，脚本在 60 秒内完成百级数据写入。 |
| FR-002 | 字段可配置映射 | `config/field_mapping.py` 中修改 Excel 列名到 Baserow field_id 的映射后，无需改代码即可适配新模板。 |
| FR-003 | 错误行与原因返回 | 脚本返回失败明细（行号、列名、失败原因）；Agent 仅向督办专员汇报汇总信息。 |
| FR-004 | 批量创建主表记录 | 一次导入 100 行以内数据时，脚本能在 60 秒内完成全部主表记录创建；记录导入不允许因经办人映射失败而整体失败。 |
| FR-006 | Baserow 自动匹配协作者 | 主表经办人字段通过 Baserow 公式/自动化匹配为 Baserow 用户，并自动写入跟进表 `Collaborators`；匹配失败时留空。 |

### FR-007 ~ FR-010：任务编号批量跟进

| 编号 | 功能 | 验收标准 |
|------|------|----------|
| FR-007 | 任务编号识别 | Agent 或脚本能从文本中提取逗号、空格、换行分隔的任意数量任务编号。 |
| FR-008 | 主表存在性校验 | 脚本对每个编号调用 Baserow API 查询，确认对应主表记录存在。 |
| FR-009 | 失败清单返回 | 不存在的编号以列表形式返回给 Agent，Agent 向督办专员说明"未找到对应任务编号"。 |
| FR-010 | 成功清单与通知 | 存在的编号创建跟进记录后，脚本返回成功编号列表；Baserow 自动匹配协作者并触发后续通知。 |

### FR-012 ~ FR-014：经办人通知

| 编号 | 功能 | 验收标准 |
|------|------|----------|
| FR-012 | 填报 URL | 通知消息中的 URL 按经办人/协作者字段筛选主表视图，经办人登录后看到全部自己被协作者的记录。 |
| FR-013 | 截止时间与操作步骤 | 消息包含明确的填报截止时间和 1-2 步操作说明。 |
| FR-014 | Bot 身份 | 消息发送者显示为 Bot 账号，不冒充任何人类用户。 |

### FR-015 ~ FR-018：24h/36h 监控提醒

| 编号 | 功能 | 验收标准 |
|------|------|----------|
| FR-015 | 自动启动 24h 检查 | 跟进记录创建后，Celery 注册 `check_follow_up_24h` 任务，延迟 86400 秒执行。 |
| FR-016 | 24h 批量提醒经办人 | 24h 后未更新记录按经办人聚合，发送一条汇总提醒消息。 |
| FR-017 | 36h 升级批量提醒 | 36h 后仍未更新，按经办人向经办人发送升级提醒，同时向督办专员发送按任务编号筛选的汇总 URL。 |
| FR-018 | 填报完成后取消 | 经办人在 36h 前完成填报（7942/7943/7944 全部非空）后，`check_follow_up_36h` 任务被取消。 |

### 新增：通知聚合与批量化

| 编号 | 功能 | 验收标准 |
|------|------|----------|
| FR-020 | 按经办人聚合通知 | 同一批次内，同一经办人的多条待办只发送一条汇总消息。 |
| FR-021 | Created on 粒度到小时 | 跟进表 `Created on` 字段精度到小时，便于按小时批次触发和聚合提醒。 |
| FR-022 | Agent 接收触发明细 | 当硬编码聚合困难时，脚本将触发数据的任务编号、经办人、协作者传给 Agent，由 Agent 按经办人整理后发送通知。 |

## P1 功能（后续迭代）

| 编号 | 功能 | 说明 |
|------|------|------|
| FR-005 | 自动创建跟进记录可配置 | 上传 Excel 后可选择是否立即创建跟进记录。 |
| FR-011 | 汇报周期去重 | 同一任务同一汇报周期内避免重复创建跟进记录。 |
| FR-019 | 可配置"已更新"判断字段 | 允许自定义监控字段，默认 7942/7943/7944 全部非空。 |
